<!DOCTYPE html>
<html>
  <head>
    <title>Proyecto Final Luis Lara</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p><%= notice %></p>
    <p><%= alert %></p>

    <% if user_signed_in? %>
      <p>
        Conectado como <%= link_to current_user.email, edit_user_registration_path %>
      </p>
      <%= link_to "Salir", destroy_user_session_path, data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to_unless_current "Registrarse", new_user_registration_path %>
    <% end %>
    
    <%= yield %>
  </body>
</html>